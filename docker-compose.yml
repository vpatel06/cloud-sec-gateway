version: '3.8'

services:
  wireguard:
    build: .
    container_name: secure-wg-gateway
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ./wireguard_config:/etc/wireguard